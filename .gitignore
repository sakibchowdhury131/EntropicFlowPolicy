.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# large training artifacts — not code, kept locally
models/
models_real/

# raw eval/demo media — curated versions live on the findings webpage
gifs/
gifs_real/

# datasets
*.zarr.zip
*.zarr_backup.zip

# stray duplicate file
efp_hard_v6 copy.py
